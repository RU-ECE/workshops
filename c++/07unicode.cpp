#include <iostream>
using namespace std;
int main() {
    // note: there are weird punctuation in different languages.
    // Chinese has characters ; , : that LOOK the same but are different.
    cout << (3 ≥ 4) << '\n';
}